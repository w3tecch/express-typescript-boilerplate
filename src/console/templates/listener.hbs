import { inject, named } from 'inversify';
import { Types } from '../../{{deepness}}constants/Types';
import { Core } from '../../{{deepness}}core/Targets';
import { Logger as LoggerType } from '../../{{deepness}}core/Logger';


export class {{name.capitalize}}Listener {

    static Event = Symbol('{{name.normal}}Listener');

    public log: LoggerType;

    constructor(
        @inject(Types.Core) @named(Core.Logger) Logger: typeof LoggerType
    ) {
        this.log = new Logger(__filename);
    }

    public run(data: any): Promise<void> {
        this.log.info('Receive event {{name.capitalize}}Listener', data);
    }

}
