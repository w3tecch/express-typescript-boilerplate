import * as Knex from 'knex';

import { Factory } from '../factories';


exports.seed = async (db: Knex) => {
    const factory = Factory.getInstance();
    // TODO: create new data with your factory
    // await factory.get(User).create(10);
};
